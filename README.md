# Character-Level-Bigram-LM
A character-level Bigram Language Model built from scratch using Python and PyTorch. It maps text to a $27 \times 27$ probability matrix, handles zero-frequency pairs with Laplace smoothing, and uses torch.multinomial to sample and generate new names. Evaluated using Negative Log Likelihood (NLL).
